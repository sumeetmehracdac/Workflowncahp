
  # Design Approval Workflow Module

  This is a code bundle for Design Approval Workflow Module. The original project is available at https://www.figma.com/design/Evax2HbAdDszNgtdzvzLeQ/Design-Approval-Workflow-Module.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  